{
  "root": "gql-macos-app",
  "projectType": "library",
  "implicitDependencies": ["gertieql", "shared"],
  "targets": {
    "build": {
      "command": "cd gql-macos-app && swift build"
    },
    "test": {
      "command": "cd gql-macos-app && SWIFT_DETERMINISTIC_HASHING=1 swift test"
    }
  }
}
