{
  "root": "ts-interop",
  "projectType": "library",
  "targets": {
    "build": {
      "command": "cd ts-interop && swift build"
    },
    "test": {
      "command": "cd ts-interop && SWIFT_DETERMINISTIC_HASHING=1 swift test"
    }
  }
}
