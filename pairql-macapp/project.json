{
  "root": "pairql-macapp",
  "projectType": "library",
  "implicitDependencies": ["pairql", "gertie"],
  "targets": {
    "build": {
      "command": "cd pairql-macapp && swift build"
    },
    "test": {
      "command": "cd pairql-macapp && SWIFT_DETERMINISTIC_HASHING=1 swift test"
    }
  }
}
