{
  "root": "pairql-dash",
  "projectType": "library",
  "implicitDependencies": ["pairql", "shared"],
  "targets": {
    "build": {
      "command": "cd pairql-dash && swift build"
    },
    "test": {
      "command": "cd pairql-dash && SWIFT_DETERMINISTIC_HASHING=1 swift test"
    }
  }
}
