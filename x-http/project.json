{
  "root": "x-http",
  "projectType": "library",
  "targets": {
    "build": {
      "command": "cd duet && swift build"
    },
    "test": {
      "command": "cd x-http && SWIFT_DETERMINISTIC_HASHING=1 swift test"
    }
  }
}
