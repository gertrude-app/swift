{
  "root": "pairql",
  "projectType": "library",
  "implicitDependencies": ["gertie"],
  "targets": {
    "build": {
      "command": "cd pairql && swift build"
    },
    "test": {
      "command": "cd pairql && SWIFT_DETERMINISTIC_HASHING=1 swift test"
    }
  }
}
